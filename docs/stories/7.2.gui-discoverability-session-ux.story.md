# Story 7.2: GUI Discoverability & Session UX

## Status

**Done**

---

## Story

**As a** GUI user,
**I want** all keyboard shortcuts documented in the Help menu, and my last-used database remembered across sessions,
**so that** I can discover power-user features and avoid repetitive setup.

---

## Acceptance Criteria

1. **AC1 (FE-018):** F1 help dialog lists ALL keyboard shortcuts grouped by context:
   - Global, Validation, Navigation Mode, Editor
   - Includes: Enter/Tab/Delete/Escape (navigation mode), Ctrl+Z/Ctrl+Shift+Z (undo/redo), Ctrl+F (filter), Ctrl++/Ctrl+- (zoom)
2. **AC2 (FE-018):** Settings screen shortcuts section updated to match
3. **AC3 (FE-018):** Navigation mode section explains activation (Enter) and deactivation (Escape)
4. **AC4 (FE-017):** After selecting a database, choice is persisted in `.gdpr-pseudo.yaml` as `default_db_path`
5. **AC5 (FE-017):** On next launch, previously used database is pre-selected in PassphraseDialog
6. **AC6 (FE-017):** Creating a new database also persists it as the new default
7. **AC7:** No regression in existing help, settings, or database detection
8. **AC8:** Unit tests for config persistence and help dialog content

---

## Tasks / Subtasks

### Task 1: Expand keyboard shortcuts registry in accessibility module (AC: 1, 2, 3)

- [x] **1.1:** In `gdpr_pseudonymizer/gui/accessibility/shortcuts.py`, **preserve all existing shortcut groups** (Global, Home, Validation, Results, Batch, Database, Settings) and **add/modify** as follows:
  - **Global** (update existing `GLOBAL_SHORTCUTS`): add Ctrl+Shift+O (Open folder), Ctrl+Q (Quit), F11 (Fullscreen) — keep existing Ctrl+O, Ctrl+B, Ctrl+,, Ctrl+D, F1
  - **Validation** (update existing `VALIDATION_SHORTCUTS`): already has Ctrl+Z, Ctrl+Shift+Z, Ctrl+F, Tab, Shift+Tab, Enter, Delete — verify completeness, no changes expected
  - **Navigation Mode** (new group `NAVIGATION_MODE_SHORTCUTS`): Enter (Activate navigation / Accept entity), Tab (Next entity), Shift+Tab (Previous entity), Delete (Reject entity), Escape (Exit navigation mode). Note: these overlap with Validation shortcuts intentionally — Navigation Mode documents the specific behavior when navigation mode is active
  - **Editor** (new group `EDITOR_SHORTCUTS`): Ctrl++ (Zoom in), Ctrl+- (Zoom out)
  - Update `get_all_shortcuts()` return dict to include the two new groups while keeping all existing groups
- [x] **1.2:** Add a description/explanation string for Navigation Mode that covers activation (Enter) and deactivation (Escape) — this is used by the help dialog to render AC3. Add as a module-level constant (e.g., `NAVIGATION_MODE_DESCRIPTION: str`).

### Task 2: Update ShortcutsHelpDialog to display all shortcut groups (AC: 1, 3)

- [x] **2.1:** Verify `ShortcutsHelpDialog` in `gdpr_pseudonymizer/gui/dialogs/shortcuts_help.py` renders all groups returned by `get_all_shortcuts()`. If it currently filters or limits groups, update it to display all.
- [x] **2.2:** Ensure the Navigation Mode group includes the explanatory text from Task 1.2 (AC3): a short paragraph explaining how to enter and exit navigation mode, placed before or after the shortcuts table for that group.
- [x] **2.3:** Verify all shortcut labels use translatable strings via `self.tr()` for i18n compatibility.

### Task 3: Update Settings screen shortcuts section (AC: 2)

- [x] **3.1:** In `gdpr_pseudonymizer/gui/screens/settings.py`, replace the hardcoded 6-shortcut list in `shortcuts_data` (line ~232) with the full shortcut list from `get_all_shortcuts()`. Import `get_all_shortcuts` from `gdpr_pseudonymizer.gui.accessibility.shortcuts`. Reuse the same data source so Settings and F1 dialog stay in sync.
- [x] **3.2:** Group shortcuts in the Settings screen by context using sub-headers or visual separators, mirroring the F1 dialog layout. Display all groups returned by `get_all_shortcuts()`.
- [x] **3.3:** Update `retranslateUi()` — the current implementation (line ~315) has hardcoded French translation strings for 6 shortcuts. Since `ShortcutDefinition.action` strings are already in French and the F1 dialog renders them directly, adopt the same approach: iterate over `get_all_shortcuts()` dynamically in `retranslateUi()` to rebuild shortcut labels, rather than maintaining a separate parallel list of `self.tr()` calls. Alternatively, wrap each `ShortcutDefinition.action` in `self.tr()` at render time so Qt Linguist can extract them for translation.

### Task 4: Persist selected database path to config (AC: 4, 5, 6)

- [x] **4.1:** In `gdpr_pseudonymizer/gui/widgets/passphrase_dialog.py`, import `save_gui_config` directly from `gdpr_pseudonymizer.gui.config` (module-level function, no constructor change needed). After the user selects or browses to a database (in `_on_db_combo_changed()`), persist the selected path to config: update `self._config["default_db_path"]` and call `save_gui_config(self._config)`. Also call `add_recent_database(path, self._config)` to keep `recent_databases` in sync. Only persist valid, existing `.db` paths (not the "Browse..." or "Create new" sentinel items).
- [x] **4.2:** After creating a new database (in the `_CREATE_ITEM` branch of `_on_db_combo_changed()`), persist the new database path to config as `default_db_path` and call `add_recent_database()` before returning from the dialog (AC6).
- [x] **4.3:** In `_populate_db_paths()`, ensure `default_db_path` from config is pre-selected (not just added to the combo box). If the config path exists and is in the combo list, set it as the current index via `setCurrentIndex()`. This ensures AC5 (pre-selected on next launch).
- [x] **4.4:** No constructor signature change needed — `PassphraseDialog` already receives `config` dict, and `save_gui_config` is imported directly as a module-level function. Verify that all 3 instantiation sites pass the live config dict (they already do):
  - `gdpr_pseudonymizer/gui/screens/home.py` (line ~249): `config=self._config`
  - `gdpr_pseudonymizer/gui/screens/batch.py` (line ~600): `config=self._config`
  - `gdpr_pseudonymizer/gui/screens/database.py` (line ~422): `config=self._config`

### Task 5: Verify no regression (AC: 7)

- [x] **5.1:** Run full test suite (`poetry run pytest tests/`) to confirm existing help, settings, and database detection still work.
- [x] **5.2:** Verify that the Settings screen still renders correctly with the expanded shortcuts list.
- [x] **5.3:** Verify F1 dialog still opens and displays correctly.

### Task 6: Unit tests (AC: 8)

- [x] **6.1:** Test config persistence — after calling `save_gui_config()` with a `default_db_path`, verify `load_gui_config()` returns the persisted path.
- [x] **6.2:** Test help dialog content — call `get_all_shortcuts()` and verify all expected shortcut groups are present (at minimum: Global, Home, Validation, Results, Batch, Database, Settings, Navigation Mode, Editor).
- [x] **6.3:** Test that Navigation Mode group contains explanatory text about Enter activation and Escape deactivation (AC3). Verify `NAVIGATION_MODE_DESCRIPTION` constant exists and contains relevant keywords.
- [x] **6.4:** Test `_populate_db_paths()` pre-selects `default_db_path` from config when the path exists.
- [x] **6.5:** Test that creating a new database updates `default_db_path` in the config.
- [x] **6.6:** Test that selecting a database via browse updates `default_db_path` in the config.

---

## Dev Notes

### Previous Story Insights

Story 7.1 (Done) added:
- Validate-once-per-entity grouping in `GUIValidationState` using `_text_group_index`
- "Masquer les validées" hide-confirmed checkbox in `EntityPanel`
- Group undo/redo via `QUndoStack.beginMacro()`/`endMacro()`
- QA noted that manually added entities (`_AddEntityCommand`) don't update `_text_group_index` — acceptable since ADDED entities aren't expected to participate in auto-grouping

**Relevant for 7.2:** The F1 help dialog and Settings shortcuts must now include the Ctrl+Z/Ctrl+Shift+Z undo/redo shortcuts and the navigation-mode shortcuts (Enter, Tab, Delete, Escape) that were already implemented but not fully documented in the UI.

### Key Source Files

**Shortcuts registry:**
- `gdpr_pseudonymizer/gui/accessibility/shortcuts.py` — Contains `get_all_shortcuts()` which returns a `dict[str, list[ShortcutDefinition]]` organized by screen/context. Currently returns 7 groups: Global, Home, Validation, Results, Batch, Database, Settings. This story adds 2 new groups (Navigation Mode, Editor) and expands Global with missing shortcuts (Ctrl+Shift+O, Ctrl+Q, F11). All existing groups and shortcuts must be preserved. This is the single source of truth consumed by both the F1 dialog and (after this story) the Settings screen.
[Source: gdpr_pseudonymizer/gui/accessibility/shortcuts.py]

**Shortcuts help dialog:**
- `gdpr_pseudonymizer/gui/dialogs/shortcuts_help.py` — `ShortcutsHelpDialog` class. Calls `get_all_shortcuts()` and renders a table per screen/group with "Raccourci" and "Action" columns. Launched from `MainWindow._show_shortcuts()` via F1 key.
[Source: gdpr_pseudonymizer/gui/dialogs/shortcuts_help.py]

**Main window:**
- `gdpr_pseudonymizer/gui/main_window.py` — Menu bar: Fichier, Affichage, Outils, Aide. The Aide menu has "Raccourcis clavier... (F1)" which calls `_show_shortcuts()`. Holds `_cached_passphrase: tuple[str, str] | None` for session passphrase caching. Loads config at init via `load_gui_config()`.
[Source: gdpr_pseudonymizer/gui/main_window.py]

**Settings screen:**
- `gdpr_pseudonymizer/gui/screens/settings.py` — Contains a "Raccourcis clavier" group box with a hardcoded 6-shortcut list:
  ```python
  shortcuts_data = [
      ("Ctrl+O", "Ouvrir un document"),
      ("Ctrl+Shift+O", "Ouvrir un dossier"),
      ("Ctrl+,", "Paramètres"),
      ("Ctrl+Q", "Quitter"),
      ("F1", "Raccourcis clavier"),
      ("F11", "Plein écran"),
  ]
  ```
  This must be replaced with data from `get_all_shortcuts()` to stay in sync with the F1 dialog.
[Source: gdpr_pseudonymizer/gui/screens/settings.py]

**Config persistence:**
- `gdpr_pseudonymizer/gui/config.py` — Config file: `.gdpr-pseudo.yaml`. Default config already includes `"default_db_path": ""`. Functions:
  - `load_gui_config()` — loads YAML, merges with `_DEFAULT_CONFIG`
  - `save_gui_config(config: dict)` — persists to YAML, preserves unknown keys
  - `add_recent_file(path)` — maintains max 10 recent files
  - `add_recent_database(path)` — maintains max 5 recent databases
  - `_config_path()` — resolves to local `.gdpr-pseudo.yaml` if it exists, else `~/.gdpr-pseudo.yaml`
[Source: gdpr_pseudonymizer/gui/config.py]

**Passphrase dialog:**
- `gdpr_pseudonymizer/gui/widgets/passphrase_dialog.py` — `PassphraseDialog` class. `_populate_db_paths()` auto-detects databases from:
  1. Same directory as selected file
  2. User home directory
  3. `default_db_path` from config (already reads it)

  Currently reads `default_db_path` but does NOT write it back after user selection. Story 7.2 must add write-back on selection and on new-database creation.

  The dialog already has `self._config` dict from constructor. For persisting, import `save_gui_config` directly from `gdpr_pseudonymizer.gui.config` (same pattern as `settings.py` line 28) — no constructor signature change needed. Also call `add_recent_database()` alongside `default_db_path` updates to keep both config keys in sync.

  **Instantiation sites** (all already pass `config=self._config`, no changes needed at call sites):
  - `gdpr_pseudonymizer/gui/screens/home.py` line ~249
  - `gdpr_pseudonymizer/gui/screens/batch.py` line ~600
  - `gdpr_pseudonymizer/gui/screens/database.py` line ~422
[Source: gdpr_pseudonymizer/gui/widgets/passphrase_dialog.py]

### Data Models

- **Config dict** — plain `dict[str, Any]` loaded from `.gdpr-pseudo.yaml`. Key fields for this story: `default_db_path: str`, `recent_databases: list[str]`.
[Source: gdpr_pseudonymizer/gui/config.py]

### File Locations

| File | Action |
|------|--------|
| `gdpr_pseudonymizer/gui/accessibility/shortcuts.py` | Modify — add Navigation Mode + Editor groups, expand Global shortcuts, add `NAVIGATION_MODE_DESCRIPTION` |
| `gdpr_pseudonymizer/gui/dialogs/shortcuts_help.py` | Modify — ensure all groups rendered, add Navigation Mode explanatory text |
| `gdpr_pseudonymizer/gui/screens/settings.py` | Modify — replace hardcoded shortcuts with `get_all_shortcuts()` data, update `retranslateUi()` |
| `gdpr_pseudonymizer/gui/widgets/passphrase_dialog.py` | Modify — import `save_gui_config`, persist selected/created DB path to config, pre-select default |
| `tests/unit/gui/test_settings.py` | Modify — add config persistence tests (Task 6.1, 6.4, 6.5, 6.6) |
| `tests/unit/gui/test_accessibility.py` | Modify — add shortcut registry and help dialog content tests (Task 6.2, 6.3) |

### Technical Constraints

- **i18n:** All user-visible strings must use `self.tr()` for French/English translation support. [Source: architecture/10-frontend-architecture.md]
- **Config path:** `.gdpr-pseudo.yaml` resolves to local file first, then `~/.gdpr-pseudo.yaml`. When saving, use the same path that was loaded from. [Source: gdpr_pseudonymizer/gui/config.py]
- **PySide6 QComboBox:** Use `setCurrentIndex()` to pre-select `default_db_path` in `_populate_db_paths()`. Do not use `setCurrentText()` as it may not match if the display text differs from the data.
- **Existing `add_recent_database()` function:** Already exists in `config.py` — call it alongside setting `default_db_path` whenever persisting a database selection, to keep both `default_db_path` and `recent_databases` in sync.

### Testing

**Test file locations (add to existing files, not new files):**
- `tests/unit/gui/test_settings.py` — add config persistence tests for Task 6.1, 6.4, 6.5, 6.6
- `tests/unit/gui/test_accessibility.py` — add shortcut registry and help dialog tests for Task 6.2, 6.3

**Testing standards:**
- Use `pytest` with `pytest-mock` for mocking Qt dependencies
- Follow existing test patterns in `tests/unit/gui/` directory
- Use `tmp_path` fixture for config file I/O tests (avoid polluting real config)
- All public methods must have type hints [Source: architecture/19-coding-standards.md]
- Use absolute imports [Source: architecture/19-coding-standards.md]
- Run all 4 quality gates before completion: `poetry run black --check`, `poetry run ruff check`, `poetry run mypy`, `poetry run pytest`

**Testing pattern for config persistence:**
- Write a config with `default_db_path` set, load it back, verify the value
- Mock `_config_path()` to use `tmp_path` for isolation
- Test round-trip: save → load → verify all keys preserved

**Testing pattern for shortcuts help:**
- Call `get_all_shortcuts()` and assert expected groups exist
- Assert specific shortcuts are present in each group (e.g., "Ctrl+Z" in Validation)
- Assert Navigation Mode group has explanatory text

---

## File List

| File | Action |
|------|--------|
| `gdpr_pseudonymizer/gui/accessibility/shortcuts.py` | Modified — added Navigation Mode + Editor groups, expanded Global shortcuts, added `NAVIGATION_MODE_DESCRIPTION`; removed Tab/Shift+Tab from VALIDATION_SHORTCUTS (they are navigation-mode-only) |
| `gdpr_pseudonymizer/gui/dialogs/shortcuts_help.py` | Modified — added Navigation Mode explanatory text, wrapped action labels in `self.tr()`; fixed light-mode readability (QFont for headers, transparent viewport/content backgrounds) |
| `gdpr_pseudonymizer/gui/screens/settings.py` | Modified — replaced hardcoded shortcuts with dynamic `get_all_shortcuts()` data, grouped by context |
| `gdpr_pseudonymizer/gui/widgets/passphrase_dialog.py` | Modified — imported `save_gui_config`/`add_recent_database`, persist selected/created DB path, pre-select default |
| `gdpr_pseudonymizer/gui/resources/themes/light.qss` | Modified — added QTableWidget/QHeaderView rules for correct light-mode colors in shortcuts dialog |
| `gdpr_pseudonymizer/gui/resources/themes/dark.qss` | Modified — added QTableWidget/QHeaderView rules (dark variants) |
| `gdpr_pseudonymizer/gui/i18n/en.ts` | Modified — added `ShortcutsHelpDialog` context with 34 English translations for all shortcut actions and dialog UI strings |
| `gdpr_pseudonymizer/gui/i18n/en.qm` | Rebuilt — recompiled from en.ts (307 translations) |
| `tests/unit/gui/test_accessibility.py` | Modified — added `TestShortcutRegistry` class (6 tests for shortcut groups + navigation mode description); updated Tab assertion to `not in` VALIDATION_SHORTCUTS |
| `tests/unit/gui/test_settings.py` | Modified — added `TestDefaultDbPathPersistence` class (5 tests: config persistence, pre-selection, browse, create, detected-db selection) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
- QA review: gate CONCERNS (FUNC-001) — `_on_db_combo_changed()` missing else branch for regular DB path selection
- Fix applied: added else branch calling `_persist_db_path(data)` for non-sentinel items
- New test added: `test_select_detected_db_updates_default_db_path` in `TestDefaultDbPathPersistence`
- All 19 `test_settings.py` tests pass post-fix; black/ruff clean
- Post-review: F1 dialog light mode — QScrollArea inline `setStyleSheet` on labels prevented theme color inheritance; fixed with QFont API and `scroll.viewport().setStyleSheet("background-color: transparent;")` + `content.setStyleSheet("background-color: transparent;")`
- Post-review: QTableWidget colors in light mode — added explicit QSS rules to light.qss and dark.qss
- Post-review: English F1 strings — added ShortcutsHelpDialog context (34 strings) to en.ts, rebuilt en.qm
- Post-review: Tab in VALIDATION_SHORTCUTS removed (Tab is only active during navigation mode, already in NAVIGATION_MODE_SHORTCUTS); test updated

### Completion Notes
- All 6 tasks completed with all subtasks checked off
- 11 new unit tests added (6 in test_accessibility.py, 5 in test_settings.py), all passing
- Quality gates: black, ruff all pass; pytest unit tests 1705 collected, all pass (pre-existing spaCy Windows segfault in integration excluded — documented known issue)
- Settings screen now dynamically renders all shortcut groups from the central registry, staying in sync with F1 dialog
- Database path persistence uses `_persist_db_path()` helper for DRY (browse + create + dropdown selection all call it)
- QA FUNC-001 resolved: else branch added to `_on_db_combo_changed()` to persist regular detected DB selections (AC4)
- F1 dialog fully readable in both light and dark themes; English translations complete

---

## QA Results

### Review Date: 2026-03-12

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Overall implementation quality is **good**. The code is clean, well-structured, and follows project conventions. The single-source-of-truth pattern for shortcuts (`get_all_shortcuts()`) is sound and DRY. The `_persist_db_path()` helper avoids duplication between browse and create paths. All 4 quality gates pass (black, ruff, mypy, pytest). 57 story-related tests pass, 10 new tests added.

**One functional gap identified**: `_on_db_combo_changed()` only persists `default_db_path` for Browse and Create branches. When a user selects an already-detected database from the dropdown (e.g., switching from file-directory DB to home-directory DB), the selection is **not** persisted. This is a gap in AC4 ("After selecting a database, choice is persisted") and Task 4.1 which explicitly requires persisting on any user selection, not just browse/create.

### Refactoring Performed

None — code quality is adequate. The issue identified requires a functional fix, not refactoring.

### Compliance Check

- Coding Standards: ✓ Absolute imports, type hints on all public functions, UPPER_SNAKE_CASE constants, PascalCase classes
- Project Structure: ✓ All modifications in expected files, tests added to existing test files
- Testing Strategy: ✓ pytest with monkeypatch for config isolation, tmp_path for file I/O, Qt mocking via qtbot
- All ACs Met: ✓ All 8 ACs fully met (AC4 fixed in v1.2)

### Improvements Checklist

- [x] **AC4 fix**: In `_on_db_combo_changed()`, add an `else` branch for regular DB paths that calls `self._persist_db_path(data)` when `data` is a valid path (not a sentinel)
- [x] **Test gap**: Add test for selecting an already-listed database updating `default_db_path` in config
- [x] Shortcuts registry has all 9 expected groups
- [x] Navigation Mode description covers Enter activation and Escape deactivation
- [x] Settings screen dynamically renders from `get_all_shortcuts()`
- [x] `retranslateUi()` dynamically rebuilds shortcut labels
- [x] Browse and Create DB paths persist correctly
- [x] Pre-selection of `default_db_path` on dialog open works

### Security Review

No security concerns. Config persistence writes to a local YAML file with no sensitive data (only file paths). No new attack surface introduced.

### Performance Considerations

No performance concerns. `get_all_shortcuts()` returns static data and is called only during UI construction/retranslation. Config persistence is lightweight YAML I/O on user action.

### Files Modified During Review

No files modified during review.

### Gate Status

Gate: PASS → docs/qa/gates/7.2-gui-discoverability-session-ux.yml

### Recommended Status

✓ Ready for Done — All 8 ACs met, all quality gates pass, 11 new tests (58 story-related total), FUNC-001 resolved.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Initial story draft — GUI discoverability (F1 shortcuts + settings sync) & session UX (DB persistence) | Bob (Scrum Master) |
| 2026-03-11 | 1.1 | PO validation fixes — clarified shortcut groups are additive (preserve existing 7 groups), listed all 3 PassphraseDialog instantiation sites, switched to direct `save_gui_config` import (no constructor change), added `retranslateUi()` i18n task, consolidated tests into existing files, added `add_recent_database()` call requirement | Sarah (Product Owner) |
| 2026-03-12 | 1.2 | QA fix — FUNC-001: added `else` branch in `_on_db_combo_changed()` to persist regular detected-DB selections (AC4); added `test_select_detected_db_updates_default_db_path` unit test | James (Dev) |
| 2026-03-12 | 1.3 | Post-review bugfixes: (1) light-mode F1 dialog — QTableWidget QSS rules in light.qss/dark.qss + transparent QScrollArea viewport fixes black-on-black section headers; (2) English F1 dialog — added ShortcutsHelpDialog i18n context to en.ts, rebuilt en.qm; (3) Tab/Shift+Tab clarification — removed from VALIDATION_SHORTCUTS (navigation-mode-only), updated test assertions | James (Dev) |
